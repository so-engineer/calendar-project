export type LoginInfoType = {
  email: string;
  password: string;
};

export type LoginReturnType = {
  id: number;
  name: string;
};

export type LoginUserType = {
  id: number;
  name: string;
};
